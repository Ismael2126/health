name: Deploy to InfinityFree via FTP

on:
  push:
    branches:
      - main  # Run this workflow only when pushing to main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3

      - name: Upload website via FTP to InfinityFree
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftpupload.net
          username: if0_39635109
          password: ${{ PodSehLYAyAX }}
          protocol: ftp
          port: 21
          local-dir: ./     # Upload everything in repo root
          server-dir: /htdocs/  # Root directory of your InfinityFree site
